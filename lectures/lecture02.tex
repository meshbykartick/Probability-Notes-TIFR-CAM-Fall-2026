\lecture
{02}
{Friday, 07 August 2026}
{Introduction to Probability}
{Dr. Nishant Chandgotia}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Todo:Change Name of this section}

Let, we have a sequence \(HT\cdotT - \) total n no.
What is the Probability of the above sequence, its \(\~ \frac{1}{2^n}\)
All sequences with k heads, \(P(k \text{Heads})=\frac{\binom{n}{k}{2^n}\) 

    Uniform Distribution of \(\{1,2,\cdot r\}\)
    If \(\left{x_{1},_{2},\ldots,x_{k}\right}\) are "Randomly" indipendently choosen then \(\sum_{n=1}^{k} X_i \) nearly equal to \(\frac{r+1}{2}\cdot k\)

    Todo: add striling approximation,

     \(\frac{P(#Heads =k)}{P(#Heads =k+1)}=\frac{k+1}{n-k}\) 

     \(\frac{k+1}{n-k}>1 iff k> \frac{n-1}{2}\ )



     We know \(\binom{n}{k}\) is maximum at \(k=\frac{n}{2}\)

     \\frac{\frac{\binom{n}{\frac{n}{2}}}{2^n} \sim \frac{\sqrt{2\pi n} {\frac{n}{e}}_n}{2.\frac{n}{2}}\\frac{n}{2e}}_n}{2^n= \frac{2}{\sqrt{2\pin} }}

It would be unjustified to say #Head =\(\frac{n}{2}\) with high probability. 
\(k=\alphan \qquad \alpha \neq \frac{1}{2}\) 


\(\frac{\binom {n}{\alpha n }}{2^n}\sim \frac{1}{\sqrt{2\pi n} }e^{n.(-\alpha \log\alpha - (1-\alpha)\log (1-\alpha)-\log_2)\)

let, \(H(\alpha)=-\alpha \log\alpha - (1-\alpha)\log (1-\alpha)\) be Boltzman Shanon Entropy (Mathematical theory of communications),
\(H(\frac{1}{2}= \log 2)\)

Todo: Add figure of  Concave Function.
By Jensen's Ineqauality (\(\log\) is Concave, \(-\alpha \log\alpha - (1-\alpha)\log (1-\alpha)<\log(2)\)

\[P(#Heads \in \left( \left( \alpha-\epsilon \right)n ,\left( \alpha+\epsilon \right)n \right))= \sum_{k=\left( \alpha -\epsilon \right)n}^{\left( \alpha+\epsilon \right)n} \frac{\binom{n}{k}}{2^n}\]

Assymptotic bounds are \(\frac{2n\epsilon}{\sqrt{2n\pi}}e_{n\left( H\left( \alpha -\epsilon \right)-H\left( \frac{1}{2} \right) \right)}\) and \(\frac{2n\epsilon}{\sqrt{2n\pi}}e_{n\left( H\left( \alpha +\epsilon \right)-H\left( \frac{1}{2} \right) \right)}\)
\(\implies P(#Heads \in \left( \left( \alpha-\epsilon \right)n ,\left( \alpha+\epsilon \right)n \right)) \to 0\) exponentially as n \(\to \infty\) 
\(P(#Heads \in n\left( \frac{1}{2}-\epsilon, \frac{1}{2}+\epsilon \right)) \to 1\)
Large deviation Phenomenon, \(P(\textrm{Staying close to mean })\to_1\)
\(P(deviating from the mean significantly )\to 0\) exponentially.

In our case rate function \(=h\left( \alpha \right) - H\left( \frac{1}{2} \right)\) 

\(f_n = \frac{#Heads}{n}\) 
\(P(\left| \frac{#Heads}{n}-\frac{1}{2} \right|>\epsilon )\to 0\)
Todo: Add definition of convergence in  measuure and probability.


\begin{question}{Question:}{some-new-problem}

\( g_n \to g in measure.
\implies g_n \text{almost everywhere}\)
\end{question}

Enumerate dyadic \(\left| \frac{k}{2_n}, \frac{k+1}{2^n} \right| \),
 \(I_{1} + I_{2}  \cdots  I_{n}\)
 \(g_k = 1_{I_k}\)

  \(g_k\to_0\) in measure.
  But \(g_k \to 0 \) almost everywhere.


\(P(\left| f_n-\frac{1}{2} \right|>\epsilon )\to 0\)
Since this is exponential decaying we will later prove that \(f_n \to \frac{1}{2}\) almost everywhere. (Borel Cantelli)

What is the measure space of \(\left\{ H, T \right\}^script{N}  \), Sigma Algebra Generated by the product topology . Basis of the topology are  cylinder sets.

Take \(k in script N \) finite  and  fix \(f: K \to \left\{ H,T \right\} \) 

\( \left[ f \right]=g \in \left\{ H, T \right\}^script{N}  \) 
\(g|_set =f\)

Todo:Some Calculation to be added.


\end{important}

alpha Beta Gamma

\begin{lecturesummary}

\item Todo: Lecture Summary Here
\item Axiomatic measure theory.
\item Law of Large Number
\item Various form of convergence, Convergence in Distribution.
\item Central limit Theorem/Large Deviation.

\end{lecturesummary}

\lectureend

